# em todos os casos, os valores de uma lista devem estar entre colchetes []

# lista preenchida estaticamente
lista_estatica = ["xpto", True]

# lista preenchida dinamicamente
lista_dinamica = [input("Digite o usuario: "), bool(int(input("Está logado? ")))]

# lista vazia
lista_vazia = []